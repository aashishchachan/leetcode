#include <bits/stdc++.h>
using namespace std;

// The class does not occupy any memory space till the time an
// object is instantiated

// Abstraction is a concept, which is allowed by encapsulation.
// You can do encapsulation without using abstraction,
// but if you wanna use some abstraction in your projects, you'll need encapsulation.